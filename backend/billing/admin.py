"""
Billing app admin module.

Note: All model registrations and admin configurations are handled in accounts.admin
to maintain a single source of truth for admin interface configuration.
This file is intentionally left minimal to prevent registration conflicts.
"""
from django.contrib import admin  # noqa